# Exported from Render on 2024-04-17T19:41:13Z
services:
- type: web
  name: Villow-ser
  runtime: ruby
  repo: https://github.com/smg478/Villow
  plan: free
  envVars:
  - fromGroup: Villow-env
  region: oregon
  buildCommand: bundle install; bundle exec rake assets:precompile; bundle exec rake
    assets:clean;
  startCommand: bundle exec puma -t 5:5 -p ${PORT:-3000} -e ${RACK_ENV:-development}
version: "1"
